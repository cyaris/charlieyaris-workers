{
	"name": "contact-form-worker",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"format": "prettier --ignore-path .gitignore --ignore-path .prettierignore --write '**/*.{css,cjs,html,js,json,jsonc,mjs,svelte,svg}'",
		"format:check": "prettier --ignore-path .gitignore --ignore-path .prettierignore --check '**/*.{css,cjs,html,js,json,jsonc,mjs,svelte,svg}'"
	},
	"overrides": {
		"undici": "^7.29.0"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.20.1",
		"prettier": "^3.9.6",
		"vitest": "^4.1.10",
		"wrangler": "^4.118.0"
	}
}
